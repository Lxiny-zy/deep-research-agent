# Dev dependencies: lint, type-check, tests
# Run: pip install -r requirements-dev.txt
-r requirements.txt

ruff>=0.15.17,<1
mypy>=1.11,<2
pytest>=8.0,<9
pytest-asyncio>=0.23,<2
pytest-cov>=5.0,<8
aiosqlite>=0.20,<1     # async SQLite for tests
httpx>=0.27,<1         # API endpoint tests (ASGITransport)
