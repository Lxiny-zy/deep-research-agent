# Dev dependencies: lint, type-check, tests
# Run: pip install -r requirements-dev.txt
-r requirements.txt

ruff>=0.6,<1
mypy>=1.11,<2
pytest>=8.0,<9
pytest-asyncio>=0.23,<2
pytest-cov>=5.0,<8
aiosqlite>=0.20,<1     # async SQLite for tests
setuptools>=77,<83     # PEP 517 build backend used by the release smoke test
wheel>=0.45,<1         # local wheel creation in CI and release checks
pip-audit>=2.9,<3     # dependency vulnerability audit in CI
