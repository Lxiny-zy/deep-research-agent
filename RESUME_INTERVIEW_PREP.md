# 简历条目与面试速记手册(Deep Research Agent)

> 定位:本文档 = 简历可直接粘贴的「项目经历」条目 + 口头介绍脚本 + 数字速记 + 口诀 +
> 高频问题速答(面试前一晚看这份)。**系统化备考用 [interview-qbank/](interview-qbank/00-总览与使用指南.md)
> 分模块题库**(6 个子系统 × 设计思路 + 四档题库);早期 44 题见
> [AGENT_INTERVIEW_GUIDE.md](AGENT_INTERVIEW_GUIDE.md),事实核对见
> [PROJECT_ANALYSIS_FOR_AGENT_RESUME.md](PROJECT_ANALYSIS_FOR_AGENT_RESUME.md)。
> 所有数字均为 2026-07-26 实测,来源见 UPGRADE_PLAN.md。

---

## 一、简历「项目经历」条目(仿 SparkOffer 排版,可直接粘贴)

**Deep Research Agent · 多智能体深度研究系统** | github.com/Lxiny-zy/deep-research-agent | 2026.06 -- 2026.07

- **项目概述**: 把一个复杂问题自动「规划拆解 → 并行检索 → 证据验证 → 反思补洞 → 综合成带引用报告」的多智能体系统。区别于常见 Deep Research 复刻的两点:报告中每条论断可点开溯源到来源网页的逐字原文与内容哈希,验证不过的论断进不了报告;执行层按分布式作业标准建设,进程强杀后可自动接管断点续跑。
- **技术栈**: 后端 FastAPI + asyncio + SQLAlchemy 2.0(async)+ Alembic;**自研 Workflow-as-Data 图编排引擎(未依赖 LangGraph)**;存储 SQLite(零配置)/ PostgreSQL;前端 React 18 + TypeScript + Vite + React Flow 可视化画布;全链路 SSE 流式;Docker Compose 与 PyInstaller 桌面版双形态交付。
- **证据链与对抗验证**: 检索→LLM 信任边界四层确定性护栏——URL 策略拒绝 + 注入信号隔离 → 逐字证据归一化匹配 + SHA-256 内容哈希 → LLM 语义支持判定 → 跨论断矛盾双向标记;验证状态由程序裁定,模型不能给自己盖章。自建 43 例对抗评测集(注入 / 伪引用 / 矛盾三类),首轮即暴露 4 类真实注入绕过(英文同义词、零宽字符拆词、中文把字句语序、URL 连字符路径),修复后**三项拦截指标全部 100%**,率值下限锁入回归测试防退化。
- **自研编排引擎**: 工作流以带版本的图数据(节点 / 条件边 / 视口)落库执行:安全条件表达式解释器、ANY/ALL/SUCCESS_ALL 三种 Join、节点级超时 / 指数退避重试 / Fallback Agent / 失败隔离与 fail-fast、run 级 token 预算与共享并发限流;内置深度 / 快速 / 复核 / 自组合 / 多团队 5 种模板,前端画布拖拽角色、连线加条件即成自定义工作流,存库后直接运行;Coordinator 角色可按问题在运行时动态生成流程。
- **作业级可靠性**: 工作流定义快照 + Blackboard checkpoint 支撑断点续跑;多实例以可续期租约 + owner 条件写 fencing 防脑裂(每次受保护写入先在同事务锁行校验持有者,续租失败立即取消执行);并行分支黑板做元素级合并,兄弟分支的追加增量在基线被改写时也不丢失。**Chaos 实测:执行中硬杀进程,重启后 1.8s 自动接管,断点续跑节省 75% 的 token 重复调用**(`make chaos-demo` 一键复现)。
- **评估与工程质量**: LLM-as-judge 四维评分 × token 成本 × 墙钟耗时的五工作流对照实验框架,支持预算受限模式下的质量曲线(reviewed 相对 quick 质量 \_\_%、成本 ×\_\_,动态编排省 \_\_% token——跑完真实对照后回填);后端 334 项 + 前端 81 项离线测试(假 LLM / 假检索注入,无需密钥与网络),Ruff / mypy / ESLint / tsc 全绿;Alembic 14 版迁移管理 schema 演进。

> 排版建议:与 SparkOffer 条目并列时放在其后(时间顺延);两个项目共有的 SSE / judge / 全栈
> 字眼在本条目里已降权,版面留给引擎、证据链与可靠性。

---

## 二、口头介绍脚本

**30 秒版**:
「我做了一个多智能体深度研究系统:提问题,系统自动拆解、并行检索、反思补洞、产出带引用的报告。它有三个别人没有的点:第一,报告里每句话都能点开看到出自哪个网页的哪段逐字原文,验证不过的论断进不了报告;第二,我用自建的对抗评测集发现并修复了 4 类 Prompt 注入绕过,注入、伪引用、矛盾三项拦截指标都是 100%;第三,执行层按分布式作业做的——进程随时 kill -9,重启 1.8 秒自动接管断点续跑,省 75% 的重复调用。整套图编排引擎是自研的,没有用 LangGraph。」

**2 分钟版**(在 30 秒版基础上展开三段):
1. **为什么自研引擎**:上一个项目用 LangGraph,我清楚框架给了什么;这个项目我想验证自己能不能把状态机、检查点、条件路由这层自己做出来。工作流是图数据不是代码——节点/条件边/Join 模式落库,画布上拖拽就能自组新流程,Coordinator 还能按问题动态生成流程。五种内置模板跑在同一引擎上。
2. **证据链怎么做的**:核心原则是"模型不能给自己盖验证章"。四层门禁全在 prompt 之外:URL 策略与注入信号是确定性规则;论断必须携带来源逐字引用,程序做归一化匹配并记录内容哈希;过了逐字关才让 LLM 判语义支持;最后跨论断做矛盾标记。前端点开引用能看到完整证据卡。为了证明它有效,我写了 43 条对抗用例去打它,第一轮就打穿了 4 类绕过——修完之后三项指标 100%,并把率值锁进了回归测试。
3. **可靠性怎么做的**:把 Agent 执行当长耗时可失败的分布式作业:节点级超时/重试/Fallback、token 预算、Blackboard 检查点;多实例竞争用数据库租约,而且不只是抢锁——每次受保护写入都在同一事务里校验租约 owner,续租失败立即取消本地任务,旧实例写不回脏数据。我做了个 chaos 演示脚本,现场杀进程给面试官看恢复过程。

---

## 三、核心数字速记表

| 数字 | 含义 | 出处 |
|---|---|---|
| **100% × 3** | 对抗评测:注入拦截 20/20、伪引用拦截 13/13、矛盾召回 10/10 | `python -m eval.adversarial` |
| **43** | 对抗用例总数(注入 20 + 伪引用 13 + 对照放行 2 + 矛盾对 10)(口头说"40+ 条") | eval/adversarial_cases.py |
| **4** | 修复的注入绕过类别:同义词 / 零宽字符 / 中文把字句 / URL 连字符 | guardrails.py |
| **1.8s / 75%** | chaos:重启后接管耗时 / 断点续跑节省的 token 比例 | make chaos-demo |
| **334 + 81** | 后端 pytest + 前端 vitest 测试数(全绿) | verify.bat |
| **5 + 5** | 内置工作流(深/快/复核/自组合/多团队)+ 内置角色(规划/研究/反思/综合/评审) | workflows.py / registry |
| **3** | Join 模式:ANY / ALL / SUCCESS_ALL | orchestration/types |
| **4 层** | 证据护栏:源策略 → 逐字 → 语义 → 矛盾 | guardrails.py |
| **14** | Alembic 迁移版本数 | alembic/versions |
| **120s / TTL** | 租约 TTL(心跳续约,过期可被接管) | api.py |

---

## 四、记忆口诀

- **总纲**:「**一链一引擎,能死能复活,句句有数字**」(证据链 / 自研引擎 / 故障恢复 / 量化指标)
- **证据链四层**:「**查源、对字、判义、标矛盾**」(SourcePolicy → EvidenceVerifier → SemanticVerifier → ConsistencyVerifier)
- **护栏铁律**:「**模型提数据,程序盖验章**」(deterministic gate 在 prompt 之外)
- **五工作流**:「**深、快、复、自、团**」(deep / quick / reviewed / auto / teams)
- **五角色管线**:「**规研反综评**」(planner → researcher → reflector → synthesizer → critic)
- **可靠性五件套**:「**照、档、栏、跳、扫**」(定义快**照** / Blackboard 存**档** / 租约围**栏** / 心**跳**续约 / 启动**扫**描恢复)
- **节点策略**:「**超时重试有退避,回退隔离看预算**」(timeout / retry+backoff / fallback / failure_policy / token budget)
- **对抗评测三类**:「**注、伪、矛**」;修复的四绕过:「**同义、零宽、把字、连字**」
- **与项目一的区隔**:「**一个会用框架,一个造了框架**」(SparkOffer 用 LangGraph 做产品闭环;本项目自研 runtime 讲基础设施)

---

## 五、高频问题速答(精选;深挖版见 AGENT_INTERVIEW_GUIDE.md)

### 架构与选型

**Q1 为什么不用 LangGraph / LangChain?**
上一个项目就是 LangGraph,我知道框架解决了什么。这个项目刻意自研,目的是验证对状态机、检查点、条件路由、并发合并这一层的理解。收益:工作流是纯数据可以落库、画布编辑、版本化;代价:自己承担正确性——所以我配了 334 项测试和 chaos 演示来兜底。两个项目合起来正好证明"会用也懂原理"。

**Q2 多 Agent 通信方式?**
共享黑板(Blackboard)模式:所有角色读写同一个结构化状态对象(plan / results / reflections / report / scratch),引擎负责在节点间传递、并行分支各拿深拷贝副本、汇合时做元素级合并。没有用消息传递,因为研究场景是"渐进丰富同一份状态",黑板比消息总线直观且易于 checkpoint。

**Q3 工作流怎么表达成数据?**
节点(角色 step + 超时/重试/回退策略)+ 条件边(安全表达式,解释器只支持白名单路径取值与比较,不 eval 代码)+ Join 模式 + 视口坐标,整体带版本号落库。保存时服务端校验:环检测、无效引用、不安全条件、唯一报告终端角色。

### 证据链与安全

**Q4 怎么防幻觉?**
不靠提示词,靠硬门禁:每条 Finding 必须带来源逐字引用,程序做 NFKC 归一化子串匹配,记录来源内容 SHA-256;只有 verified 且语义 supported 的论断能进反思/综合和最终引用。LLM 输出被当作"不可信提案"。

**Q5 怎么防 Prompt 注入?**
分层:确定性信号规则(中英文注入模式、URL path/query/fragment 解码后扫描、零宽字符归一化)命中即隔离该来源;即使漏网,注入内容也编不出通过逐字验证的论断——信号层缩小暴露面,证据层是底线。我用对抗评测证明这套有效:首轮 80% 拦截,暴露 4 类绕过,修复后 100%。

**Q6 三个 100% 会不会太满?(必被追问)**
主动给边界:100% 是"在我构造的 43 条用例上",不是宣称通用免疫——黑名单式检测本质不完备,同形字等变体理论上仍可绕过。这套评测的价值是建立了"红队→修复→锁指标防回归"的闭环,而不是数字本身。真正兜底的是逐字证据门禁:它的 100% 是机制性的(不匹配就是不匹配)。残余风险在"来源本身是假话"——逐字验证证明引用真实存在,不证明来源可信,来源信誉分级是 roadmap。

### 可靠性

**Q7 断点恢复怎么做?语义是什么?**
图层/步骤边界写 checkpoint(Blackboard + 每步状态 + 工作流定义快照);恢复时用快照定义而非现行定义(防用户中途改了工作流),已终态步骤跳过。语义是 **at-least-once**:外部调用完成但 checkpoint 未落盘时崩溃,恢复后该节点会重调——研究型只读调用的代价是重复成本而非副作用,诚实说清这不是 exactly-once。

**Q8 多实例怎么防脑裂?**
租约(owner + 过期时间 + 心跳续约)只是第一层;关键是 **fencing:每次受保护写入在同一事务内先空更新锁 workflow_run 行并校验 lease_owner,不匹配抛 LeaseLostError**;续租失败会立即取消本地执行任务。所以旧实例即使还活着也写不回状态。再进一步的增强是单调递增 fencing token(防同 owner 复用的 ABA 边界),我知道差距在哪。

**Q9 并行分支状态冲突怎么合并?**
每分支拿基线快照的深拷贝,汇合时逐 key 元素级合并:基线段以分支对基线的差异覆盖,各分支超出基线的追加段全部保留。这里我实际修过一个 bug:两个分支同时改写基线前缀时,旧实现会把先合并分支的追加整段丢掉——用真实 Blackboard 复现、修复、写回归测试,这是我对"并发状态合并"理解最深的一课。

### 评估

**Q10 效果怎么量化?**
两套:①编排对照——五种工作流在同一用例集上跑 LLM-as-judge 四维评分(覆盖/可靠/深度/可读)× token × 墙钟,支持预算受限模式的质量曲线,结论落 markdown;②对抗评测——护栏三项拦截率。judge 在这里是度量工具,判分维度和用例 notes 是我设计的。

**Q11 测试没有真实 LLM 怎么可信?**
依赖注入:LLM 和检索是接口,测试注入行为可控的假实现,离线跑完整个编排/恢复/护栏链路(334+81 项,无需密钥)。假后端测的是**我写的系统逻辑**——调度、合并、门禁、恢复——这些正是会出 bug 的地方;模型好坏另由 eval 层用真实 LLM 度量,两层职责分开。

### 前端(简答即可,别展开)

**Q12 前端亮点?**
证据链可视化:引用点开侧栏看逐字原文/验证徽章/哈希/矛盾链接;React Flow 画布工作流构建器(拖角色/连条件边/存库即跑);SSE 用 fetch 流+Header(密钥不进 URL),断线由轮询兜底,多字节分块解析有专门处理。

### 兜底与反问

**Q13 项目最大的不足?**
①执行仍在 FastAPI 进程内,没有独立 Worker/消息队列,水平扩缩容受限——租约体系已为此铺路;②编排对照的真实数字还没跑完(框架就绪);③来源可信度没有分级。主动说边界比装完美可信。

**Q14 如果重来一次?**
先写对抗评测再写护栏(测试驱动安全);黑板 scratch 早点上 schema 约束;事件流从第一天就设计成可回放的审计日志(现在是演进出来的)。

---

## 六、面试现场演示清单(30 秒内可启动)

1. `make chaos-demo` —— 杀进程→自动接管→节省 75% 的完整输出
2. `python -m eval.adversarial` —— 三项 100% 的对抗评测表
3. 前端跑一条研究 → 点报告引用开证据卡 → 指给面试官看 verified 徽章与哈希
4. 工作流画布:拖一个角色、连一条条件边、保存并运行

> 演示环境离线可跑:`DR_DEMO_FAKE_BACKENDS=1` + 假密钥即可,无需真实 API。
