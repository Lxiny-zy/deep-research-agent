# 本地代码审计与修复说明

## 审计范围

本次审计覆盖多 Agent 编排运行时、DAG 调度、条件路由、节点可靠性策略、Blackboard
并发合并、WorkflowRun/StepRun 持久化、Checkpoint 恢复、FastAPI 启动恢复、SSE 事件回放、
Alembic 迁移及前端工作流画布。

## 已修复问题

### 1. 线性工作流恢复时重复执行已完成节点

**问题**：图工作流能够根据 `node_id` 跳过已完成节点，但内置 `deep/quick/reviewed` 等线性
工作流在恢复时会从第一步重新执行，可能重复调用 LLM、重复搜索并增加 Token 消耗。

**修复**：恢复线性工作流时，根据定义快照中的步骤顺序与已持久化 StepRun 前缀进行匹配；
状态为 `SUCCEEDED/FAILED/SKIPPED` 的步骤直接跳过，只执行第一个未完成步骤及其后续步骤。

**原理**：Checkpoint 已包含已完成步骤对 Blackboard 的影响，因此恢复时重放状态而不是重放副作用。

### 2. 恢复任务覆盖崩溃前的事件历史

**问题**：仓储的 `save_events` 采用覆盖写语义。恢复任务使用新的 Tracer，如果不加载旧事件，
任务结束时会把崩溃前的 Agent 时间线全部覆盖。

**修复**：恢复执行前从数据库读取原事件并注入新 Tracer，同时回填 EventHub 缓冲区；新事件在旧
事件之后继续追加，最终覆盖写入的是完整事件序列。

**原理**：维持单调事件序列，使数据库回放、恢复期间 SSE 和恢复后的历史详情保持一致。

### 3. Fail-fast 与取消状态没有即时写入 Checkpoint

**问题**：节点成功时会保存 Checkpoint，但 `fail_fast` 或任务取消会直接抛出异常，最终 FAILED/
CANCELLED 状态可能没有写入 WorkflowRun。进程再次启动时可能误判为仍在运行。

**修复**：WorkflowEngine 在根工作流异常或取消时，先转移 WorkflowRun 终态，再调用异步
Checkpoint sink 持久化 Blackboard、StepRun 和终态，然后继续传播原异常或取消信号。

**原理**：采用“先记录终态，再传播失败”的顺序，保证恢复判断以数据库状态为准。

### 4. 前端编辑非拓扑顺序图时节点与步骤错配

**问题**：后端 `steps` 是拓扑排序结果，而 `nodes` 保留用户声明顺序。如果前端使用
`steps[index] + nodes[index].id` 重建画布，非拓扑顺序保存的图会把 Agent 配置绑定到错误节点。

**修复**：存在图定义时，编辑器直接以 `nodes[].step` 为节点内容来源；仅旧工作流没有 nodes 时
才回退到 `steps[]`。

**原理**：节点 ID、坐标和节点配置必须来自同一个聚合对象，拓扑步骤只用于旧运行时兼容。

### 5. 恢复期间 SSE 缺失旧事件

**问题**：即使 Tracer 保留旧事件，新建立的 EventHub 仍只收到恢复后的事件，恢复期间连接的前端
看不到此前执行阶段。

**修复**：创建恢复任务时把数据库历史事件发布到新的 EventHub 缓冲区，再注册实时 sink。

**原理**：EventHub 对新订阅者先回放缓冲事件，再发送实时事件，从而形成连续时间线。

### 6. 恢复跳步误伤 Compose 与 Team Fan-out 子流程

**问题**：初版线性恢复逻辑根据 Runtime 中已有 StepRun 跳过步骤，但 compose 和 team fan-out 会在
同一个 Runtime 中递归执行子工作流。普通运行时，子流程错误地把父流程 StepRun 当成自己的恢复记录，
从而跳过 Planner/Researcher，最终报告没有研究来源和引用。

**修复**：只有同时满足“传入了 resume_run”与“当前执行的是根工作流”时才启用恢复匹配；嵌套
工作流始终正常执行。图工作流的 node_id 恢复映射也应用相同边界。

**原理**：恢复游标属于一次根 Workflow Definition，不能仅凭共享 Runtime 中存在历史步骤就推断
任意递归子流程处于恢复状态。

## 保留的边界与约束

- 任意图回边仍禁止；循环通过有最大轮次和 Token Budget 的 `reflect_loop` 节点实现。
- 条件表达式不使用 `eval`，只支持状态路径、JSON 字面量和有限比较运算符。
- 同层并行节点使用 Blackboard 深拷贝，并按节点声明顺序确定性合并。
- 多入边当前采用“任一激活即可执行”的路由汇聚语义；需要全部前驱成功的严格 Join 可作为独立
  节点类型继续扩展。
- 恢复使用运行时保存的 Workflow Definition 快照，不使用可能已被编辑的新版本。

## 第二轮生产边界与 UI 修复

### 7. 多入边缺少明确的 Join 语义

**问题**：所有汇聚节点原本固定使用“任一输入激活即可运行”，适合条件分支，但不适合必须等待并
确认所有研究分支成功的聚合节点。

**修复**：节点新增 `join_mode`：`any`、`all`、`success_all`。画布在节点存在多路输入时提供汇聚
策略选择，运行时分别根据激活状态和成功状态判断节点是否执行。

### 8. 多实例可能同时恢复同一个任务

**问题**：多个 API 实例启动时会同时扫描 pending/running 任务，可能重复恢复、重复调用模型。

**修复**：WorkflowRun 新增数据库租约 `lease_owner/lease_expires_at`；恢复前通过原子 UPDATE 抢占，
执行期间定时续约，结束后释放。其他实例抢占失败时跳过该任务。

### 9. Checkpoint 删除并重建 WorkflowRun 导致写放大

**问题**：每次 Checkpoint 都删除 WorkflowRun 并级联删除 StepRun，再以相同 ID 重建，不利于租约、
外键稳定性和数据库写入效率。

**修复**：WorkflowRun 改为原位 UPDATE，仅替换该运行的 StepRun 集合；租约字段不会被 Checkpoint
覆盖或清空。

### 10. 编排工作室在窄窗口发生遮挡和横向溢出

**问题**：约 1050px 以下仍维持多栏，检查器会覆盖画布；手机宽度下节点、表单和操作区无法同时
到达。

**修复**：宽屏保留角色库/画布/检查器三栏；1050px 以下改为三视图标签切换；680px 以下使用
`100dvh` 全屏编辑、单列可靠性表单、流式节点宽度和紧凑操作栏。运行管线保留横向滚动，避免压缩
节点内容到不可读。

### 11. 桌面端主体被放入零高度 Grid 行

**问题**：为移动标签栏预留了一个高度为 0 的 Grid 行，但桌面端标签栏 `display:none` 后不再参与
Grid 排版，主体自动占用了该零高度行。三个区域只显示溢出的标题，角色、节点和表单全部被裁掉。

**修复**：桌面端恢复为“标题栏 + 主体”两行；只有在 1050px 以下移动标签栏实际显示时，才切换
为“标题栏 + 标签栏 + 主体”三行。

**原理**：CSS Grid 的 `display:none` 子元素不会生成 grid item，不能依赖隐藏元素占据预设行。

### 12. 画布交互仍受线性列表限制

**问题**：旧编辑器虽然能配置依赖，但节点只能沿纵向管线显示，缺少自由摆放、端口连线、缩放平移
和全局导航，复杂图编辑成本高。

**修复**：接入专业节点画布，支持自由拖动、连接端口、平移缩放、MiniMap、条件边动画和节点位置
持久化；依赖检查器保留为精确配置入口。

### 13. 模型档案依赖手工填写模型 ID

**问题**：用户需要记忆 provider 的模型名称，容易输入不存在或拼写错误的模型；测试连接只能在保存
档案后执行。

**修复**：新增临时配置测试和远端模型发现 API。编辑器可使用当前 Base URL/API Key 或已有档案
密钥拉取 OpenAI-compatible `/models` 列表，搜索并下拉选择，然后在保存前执行最小模型调用测试。

### 14. 新节点自动串联导致画布仍表现为线性流程

**问题**：从角色库添加节点时默认依赖上一个节点，用户即使使用自由画布，得到的仍然是线性链；连线
也缺少删除入口。

**修复**：新增节点默认保持孤立，由用户通过端口决定分支、串行和汇聚。连线支持选中后按 Delete/
Backspace 删除，也支持双击删除；节点可自由拖动、缩放和平移。

### 15. 推理模型被强制发送 temperature

**问题**：模型档案只有 temperature，LLM 封装在 complete/parse/stream 中始终发送该参数。部分推理
模型不接受 temperature，而要求 reasoning effort。

**修复**：模型档案新增 `parameter_mode` 和 `reasoning_effort`。温度模式只发送 temperature；推理
模式完全省略 temperature，改发 low/medium/high reasoning effort。测试连接使用相同参数路径。

## 复杂研究功能状态

复杂研究功能仍然保留，并没有被多 Agent 编排替代。现有深度研究模板仍包含：

1. Planner 拆解复杂问题并生成带依赖关系的子问题；
2. Researcher 使用搜索工具获取来源并提取带 URL 的证据；
3. 子问题通过 DAG 分层并行执行；
4. Reflector 判断证据充分性并生成补充研究问题；
5. `reflect_loop` 在轮次与 Token Budget 内执行补充研究；
6. Synthesizer 汇总发现、生成 Markdown 报告及编号引用；
7. 研究计划、来源、结果、报告、事件和执行状态全部持久化。

多 Agent 编排层现在是这些研究能力的运行底座；`deep`、`quick`、`reviewed` 等研究流程仍可直接运行，
自定义画布只是新增了组合和扩展这些能力的方式。

## 验证方式

- `python -m pytest -q`
- `python -m ruff check deep_research tests`
- `python -m mypy deep_research`
- `npm run lint`
- `npm test`
- `npm run build`
- `alembic heads`
