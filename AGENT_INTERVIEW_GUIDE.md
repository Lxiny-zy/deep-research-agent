# Deep Research Agent：简历亮点与面试题详解

> 适用方向：AI Agent 工程师、LLM 应用工程师、Agent 平台/编排工程师、Python 后端工程师。
>
> 本文基于项目当前代码实现整理。重点不是把项目描述成“调用几个模型的 Demo”，而是讲清楚它如何解决 Agent 系统中的编排、证据可靠性、并发一致性、故障恢复、成本控制和可观测性问题。

## 一、项目一句话介绍

设计并实现了一套可配置的多 Agent 深度研究系统：将复杂问题拆解成带依赖的子问题，通过 DAG 分层并发检索、来源策略门禁、原文证据验证、反思补洞和引用约束生成研究报告；同时提供可视化工作流编排、节点级可靠性策略、Checkpoint 恢复、SSE 实时观测、运行历史回放和 LLM-as-judge 自动评估。

核心技术栈：Python 3.11、FastAPI、Pydantic、asyncio、OpenAI-compatible API、SQLAlchemy 2.0、SQLite/PostgreSQL、Alembic、React、TypeScript、React Flow、SSE、pytest。

---

## 二、可直接放入 Agent 岗位简历的三条核心创新亮点

### 亮点一：构建“Workflow-as-Data”的分层多 Agent 编排引擎

#### 简历精炼版

- 设计可声明、可生成的多 Agent 编排引擎，以统一 `Agent.step(Blackboard, RunContext)` 协议解耦角色与控制流，支持线性流程、DAG 分支/汇聚、条件路由、反思循环、运行时自组合及多团队 Map-Reduce；通过拓扑分层和隔离子黑板实现同层安全并发与确定性合并。

#### 项目解决了什么问题

传统 Agent Demo 通常把 `Planner → Researcher → Synthesizer` 写死在一个函数中，存在以下问题：

1. 新增角色需要修改编排器；
2. 不同角色方法签名不一致，无法通用调度；
3. 难以支持分支、条件、汇聚、重试和动态组队；
4. 并行 Agent 直接写共享状态容易产生竞态；
5. LLM 动态生成流程可能引用不存在的角色、无限递归或没有终端输出。

#### 项目的实现方案

1. **统一角色协议**
   - 所有角色统一实现 `async step(bb, ctx) -> Blackboard`。
   - `Blackboard` 保存计划、研究结果、反思、报告和扩展 `scratch`。
   - `RunContext` 注入 LLM、搜索工具、Tracer、配置和角色专属模型解析器。

2. **控制流数据化**
   - `Workflow`、`Step`、`WorkflowNode`、`WorkflowEdge` 描述流程，不把控制流写死在业务代码中。
   - 引擎可解释线性步骤，也可对图执行拓扑分层。
   - 自定义角色由数据库角色卡片驱动，通过 `behavior + system_prompt + model_profile` 复用内置执行能力，无需修改引擎。

3. **DAG 并发调度**
   - Kahn 算法生成拓扑层，同层节点通过 `asyncio.gather` 并发执行，层间保持依赖顺序。
   - 每个并行节点使用 `Blackboard.model_copy(deep=True)` 得到隔离状态，避免并发写父黑板。
   - 一层完成后按节点声明顺序串行合并增量结果，保证结果可重复、可测试。

4. **明确的汇聚和条件语义**
   - 边条件使用受限表达式解释器，只支持状态路径、JSON 字面量和有限比较运算，不使用 `eval`。
   - 节点支持 `any`、`all`、`success_all` 三种 Join 模式，分别解决条件分支、全部激活和全部成功后汇聚的问题。

5. **受约束的自主编排**
   - Coordinator 可以让 LLM 现场生成步骤列表。
   - 生成结果必须经过角色白名单、步骤数、反思轮数、终端角色、禁止嵌套 compose/team-fanout 等规则校验。
   - 首次不合法时将错误反馈给模型自修复；仍失败则回退内置 `deep` 流程。
   - 对零研究产出的动态流程设置有限重规划次数，并受 Token Budget 共同约束。

#### 为什么具有创新性

创新点不只是“支持多个 Agent”，而是把 Agent、状态和控制流拆成三个可独立演进的层次：角色是能力插件，Blackboard 是显式状态，Workflow 是可保存、可生成、可恢复的数据。这使同一引擎既能运行固定研究模板，也能运行用户在画布上创建的 DAG，还能执行 LLM 动态生成的流程。

---

### 亮点二：构建证据约束的研究闭环，而非单轮内容生成

#### 简历精炼版

- 实现“问题拆解—依赖检索—来源门禁—证据验证—反思补洞—引用综合”的闭环：对子问题进行 DAG 并行检索，在内容进入 LLM 前执行 URL 与 Prompt Injection 策略（包括 URL path/query/fragment）；要求 Finding 携带来源原文 quote，由程序验证匹配、语义支持和跨论断一致性，只有 `verified + supported` 论断可进入报告；通过 Reflector 有界补充证据，并以 LLM-as-judge 对比工作流质量/Token 成本。

#### 项目解决了什么问题

普通 RAG 或搜索增强生成经常存在：

1. 只搜索一次，复杂问题覆盖不足；
2. 模型可能把网页中的提示注入当作指令；
3. 模型可能生成来源列表中不存在的 URL；
4. 报告引用编号与来源无法稳定对应；
5. 只能凭主观感受判断某种 Agent 流程是否更好。

#### 项目的实现方案

1. **带依赖的问题拆解**
   - Planner 输出结构化 `ResearchPlan`，每个子问题可声明 `depends_on`。
   - 对越界依赖、自环和重复依赖进行清洗。
   - 调度器检测环；对于模型生成的子问题依赖环，降级为纯并行，避免死锁。

2. **分层并发检索**
   - 无依赖子问题同层并发，依赖后续问题在前驱完成后运行。
   - 前驱发现只作为后续问题的理解背景，不能冒充新发现来源。
   - `Semaphore` 控制最大检索并发，避免打爆模型或搜索 API。

3. **引用与来源约束**
   - SourcePolicy 在网页内容进入 LLM 前拒绝非 HTTP(S)、非公网/歧义 IP、内部主机名和嵌入凭据 URL，并隔离标题、正文或 URL path/query/fragment 命中中英文 Prompt Injection 规则的来源。
   - 模型输出结构化 `FindingList`，每条 Finding 必须带 `source_url` 和逐字复制的 `evidence_quote`。
   - EvidenceVerifier 先检查 URL 白名单，再对 quote 与对应 Source 做 Unicode/空白归一化匹配；验证状态和来源内容 SHA-256 由程序生成，模型不能自行授信。
   - SemanticEvidenceVerifier 只在确定性 quote gate 之后判断原文是否真的支持论断；unsupported/uncertain 不进入反思和综合材料。
   - ClaimConsistencyVerifier 为已支持论断生成稳定 claim_id，并标记跨来源矛盾对；conflicted 论断保留冲突链路和原因，供报告显式呈现争议。
   - 策略决策、候选数、拒绝数和拒绝原因进入 Tracer 事件，支持 SSE 观察与历史审计。
   - Synthesizer 只接收 `verified + supported` 发现，由系统建立稳定的 `URL → [n]` 映射并自动追加参考来源，模型不负责自行编造引用列表。

4. **反思补洞闭环**
   - Reflector 判断证据是否足以回答原始问题。
   - 若不足，输出缺口和最多三个新子问题。
   - 新问题通过 `pending_sub_questions` 增量交给 Researcher，不重复执行已有研究。
   - 反思轮数和 Token Budget 同时提供终止保证。

5. **自动化质量评估**
   - LLM-as-judge 从 coverage、groundedness、depth、coherence 四个维度评分。
   - 评估工具可以对比 `deep`、`quick`、`auto` 等工作流的平均质量和平均 Token。
   - Judge 使用独立 Tracer，避免评估调用污染被测 Agent 的成本统计。

#### 为什么具有创新性

项目把“检索到内容”进一步推进到“证据是否充分、引用是否真实、流程质量是否可以回归验证”。它通过模型约束与代码校验双层防线降低幻觉，并把反思结果转化为可执行的新研究任务，而不是只让模型输出一句自我评价。

---

### 亮点三：实现面向生产的可恢复执行与全链路可靠性

#### 简历精炼版

- 为长耗时 Agent 任务设计生产级执行保障：节点级超时、指数退避重试、Fallback Agent、失败隔离/Fail-fast、Token Budget、状态机和 Checkpoint；通过工作流定义快照、Blackboard/StepRun 持久化及数据库恢复租约实现进程中断续跑和多实例防重复恢复，并以 Tracer/EventHub/SSE 支持实时观测、迟到订阅回放与历史审计。

#### 项目解决了什么问题

长链路 Agent 任务可能持续数分钟，并调用多个外部服务。常见风险包括：

1. 单节点超时或限流拖垮整条工作流；
2. 客户端断开后服务仍持续消耗 Token；
3. 服务重启导致任务从头执行，产生重复调用和重复费用；
4. 多个实例同时恢复同一个任务；
5. 并发和恢复状态缺乏明确生命周期，无法审计；
6. SSE 慢消费者导致内存无限增长或反向阻塞主流程。

#### 项目的实现方案

1. **节点级可靠性策略**
   - `timeout_seconds` 使用 `asyncio.timeout`。
   - `max_attempts + retry_backoff * 2^attempt` 实现有限指数退避。
   - 主 Agent 多次失败后可切换 `fallback_agent`。
   - 默认隔离单节点失败继续执行；关键节点可设置 `fail_fast`。
   - 搜索 Key 池对配额、鉴权、限流错误执行粘滞故障转移。

2. **显式状态机**
   - Workflow 状态：pending、running、succeeded、failed、cancelled。
   - Step 状态：pending、ready、running、retrying、succeeded、failed、skipped、cancelled。
   - 状态转换有白名单约束，非法转换立即报错。
   - 预算耗尽和条件不匹配会记录为 `skipped`，而不是让节点从历史中消失。

3. **成本和终止控制**
   - Tracer 是 Token 消耗的唯一真相源，TokenBudget 同步累计值，避免重复计费。
   - 预算耗尽后跳过非终端节点，但仍允许 Synthesizer/Aggregator 尽力生成报告。
   - compose 深度、生成步骤数、重规划次数、反思轮数均有上限。

4. **Checkpoint 与恢复**
   - 每层或每步后保存 Blackboard、WorkflowRun、StepRun 和 Workflow Definition 快照。
   - 恢复使用运行时保存的定义快照，不使用后来可能被编辑的工作流版本。
   - 已处于 succeeded/failed/skipped 的根流程节点不会重复执行；Checkpoint 已包含其状态影响。
   - 取消或 Fail-fast 时先写入终态 Checkpoint，再传播异常，避免任务被误判为仍在运行。

5. **多实例恢复租约**
   - 数据库使用原子 `UPDATE ... WHERE lease is null/expired/owned by self` 抢占恢复权。
   - 执行期间心跳续租，完成后释放。
   - 解决多 API 实例启动时重复恢复、重复调用 LLM 的问题。

6. **事件驱动可观测性**
   - Tracer 将 Agent 行为统一建模为 Event，同一事件流同时服务 CLI、SSE、前端和数据库回放。
   - EventHub 为每个 SSE 订阅者提供独立有界队列，支持多端同时观看。
   - 非 Token 事件缓冲并持久化；Token 增量只实时传输，避免数据库和内存膨胀。
   - 慢消费者队列满时丢弃事件，不反向阻塞主研究任务；最终报告和详情接口可兜底恢复状态。

#### 为什么具有创新性

大多数 Agent 项目只关注 Prompt 和结果，本项目把 Agent 执行视为一种长耗时、可失败、需要恢复的分布式作业。Checkpoint、状态机、租约、事件回放和成本预算共同构成了从 Demo 到可运维系统的关键能力。

---

## 三、面试开场介绍模板

### 30 秒版本

这个项目是一套多 Agent 深度研究与工作流编排系统。它把 Agent 能力、共享状态和控制流解耦，通过统一协议运行固定流程、用户可视化 DAG 和 LLM 动态生成流程。研究链路支持带依赖的并发检索、证据反思补洞和可验证引用；工程上实现了节点重试、Fallback、Token Budget、Checkpoint 恢复、数据库租约和 SSE 全链路观测，因此重点不只是“多 Agent”，而是 Agent 系统的可靠执行与可评估性。

### 2 分钟版本

项目最初解决的是复杂问题的一次性生成质量不足。我把研究过程拆成 Planner、Researcher、Reflector、Synthesizer 等职责明确的 Agent：Planner 输出带依赖的子问题，调度器按 DAG 分层并发检索，Reflector 判断证据缺口并增量补洞，Synthesizer 只基于验证过的 Finding 生成带编号引用的报告。

之后我进一步把固定流程抽象为 Workflow-as-Data。所有角色统一实现 `step(Blackboard, RunContext)`，引擎既支持线性步骤，也支持可视化 DAG、条件边、Join、自组合和多团队并行。并行节点不共享可变黑板，而是深拷贝后独立执行，再按声明顺序确定性合并。

考虑到 Agent 调用时间长、外部依赖多，我又加入节点级超时、指数退避、Fallback、Fail-fast、Token Budget、运行状态机和 Checkpoint。服务异常重启后可以基于定义快照和 Blackboard 恢复；多个实例通过数据库租约避免重复恢复。全过程通过统一事件流推送到 SSE，并落库用于历史回放。最后用 LLM-as-judge 对不同工作流的质量和 Token 成本做自动化对比。

---

## 四、围绕亮点一的面试题与详细答案

### 1. 为什么要用 Blackboard，而不是让 Agent 之间直接互相调用？

**答案：**

直接调用会让角色之间形成强耦合，例如 Planner 必须知道 Researcher 的参数，Researcher 又必须知道 Synthesizer 需要什么格式。新增角色或改变流程时需要修改多个类。

项目将运行状态集中到 Pydantic `Blackboard`：核心字段包括 query、plan、results、reflections、report，非通用扩展放入 scratch。所有 Agent 只遵循统一 `step(bb, ctx)` 协议，编排引擎只负责搬运状态和控制执行顺序，不理解具体业务。这使角色可以被注册表、数据库角色卡片或不同工作流复用。

边界上，Blackboard 不能变成无限扩张的“万能字典”。稳定的跨角色契约应进入强类型字段；实验性或角色私有数据才放 scratch，并需要命名约定防止键冲突。

### 2. DAG 为什么采用拓扑分层，而不是遇到节点就创建 Task？

**答案：**

拓扑分层容易表达“层内并发、层间依赖”，也便于把前层结果作为后层上下文。项目使用 Kahn 算法计算层级，再对每层调用 `asyncio.gather`。

如果逐节点动态调度，理论并发度可能更高：某个节点的所有前驱完成即可立即启动，不必等待同层其他慢节点。但实现需要维护入度、完成事件、任务队列和取消传播，恢复与确定性也更复杂。当前研究任务的节点数量有限，分层方案用少量并发效率换取简单、可观测和可恢复性，是有意识的工程取舍。

### 3. 多个并行 Agent 同时修改 Blackboard，怎么避免竞态？

**答案：**

项目没有让并行节点直接写共享对象。每个节点执行前对父 Blackboard 做深拷贝，节点在子黑板中独立运行。`gather` 完成后，引擎按照节点声明顺序合并：列表字段只追加相对基线产生的增量，plan/report 采用明确覆盖规则，scratch 按顺序更新。

这种方式避免锁和并发写，但也有边界：两个节点同时修改同一个非追加字段时，后合并者覆盖前者。因此对于需要语义化合并的字段，应定义 Reducer，而不能长期依赖通用 `dict.update`。如果系统规模扩大，可以给 Blackboard 字段注册 merge strategy，例如 append、first-wins、last-wins、set-union 或自定义 reducer。

### 4. `any`、`all`、`success_all` 有什么区别？

**答案：**

- `any`：任一入边激活即可执行，适合互斥条件分支后的汇聚；
- `all`：所有入边条件都激活才执行，但前驱节点可以是失败状态；
- `success_all`：所有入边都激活且所有前驱都成功，适合必须收齐全部研究结果的严格聚合。

项目分别维护 active_nodes 和 successful_nodes，并结合边条件计算节点是否运行。未满足条件的节点进入 skipped 状态，保留完整审计记录。

### 5. 为什么条件表达式不直接使用 Python `eval`？

**答案：**

工作流条件来自用户输入或持久化数据，使用 eval 会带来任意代码执行风险。项目实现了一个最小条件语言：正则只接受状态路径和 `== != > >= < <=`，右值只能是 JSON 字面量，并通过安全路径读取器访问 Blackboard 序列化状态。

这牺牲了表达能力，但安全边界清晰。复杂条件应通过受信任的路由 Agent 或新增受控操作符实现，而不是开放任意 Python。

### 6. LLM 动态生成工作流，如何防止无限循环或生成非法流程？

**答案：**

项目使用多层约束：

1. 角色必须在注册白名单中；
2. 生成步骤数不超过上限；
3. reflect_loop 轮数不能超过全局上限；
4. 必须包含能生成报告的终端角色；
5. 禁止生成流程再次使用 compose 和 team_fanout，防止递归爆炸；
6. compose 深度限制为一层；
7. 非法结果先反馈错误让模型修复，仍失败回退到内置 deep；
8. 零产出流程只允许有限次数重规划，并受 Token Budget 限制。

这体现了 Agent 自主性应放在确定性护栏内部，而不是完全相信模型输出。

### 7. 如何扩展一个新的 FactChecker Agent？

**答案：**

代码型扩展只需实现 `name` 和 `async step(bb, ctx)`，通过注册器注册，然后在 Workflow 的 agent 字段引用该名字。引擎不需要修改。

数据型扩展可以新增角色卡片，选择现有 behavior、填写 system_prompt 并绑定模型档案。`CardAgent` 会委托对应内置实现，同时使用卡片名称解析专属 LLM。

如果 FactChecker 需要全新的执行逻辑，而不是 Prompt 变体，就应该新增 behavior implementation，而不应强行复用不匹配的 Researcher 或 Critic。

### 8. 为什么项目同时保留线性工作流和图工作流？

**答案：**

这是渐进迁移与向后兼容设计。旧的内置 `deep/quick/reviewed` 使用 steps，简单、稳定；新的用户工作流使用 nodes/edges，支持分支和汇聚。引擎根据是否存在 nodes 选择执行路径，后端保存时还会同步拓扑 steps，兼容已有数据和调用方。

长期可以统一成图模型，因为线性流程本质上是每个节点只有一个后继的特殊 DAG。但迁移期间保留适配层可以降低一次性重构风险。

---

## 五、围绕亮点二的面试题与详细答案

### 9. 这个项目与普通 RAG 的区别是什么？

**答案：**

普通 RAG 常见流程是一次检索、拼接上下文、生成答案。本项目增加了计划、依赖调度、证据抽取、反思补洞、引用校验和多流程评估：

- Planner 决定查什么；
- DAG 决定哪些问题并行、哪些依赖前驱；
- Researcher 将网页转成结构化 Finding；
- Reflector 判断还缺什么，并产生新任务；
- Synthesizer 只消费验证后的证据；
- Judge 衡量质量与成本。

因此它更接近一个有状态、有反馈闭环的研究系统，而不是一次 Prompt 链。

### 10. 如何保证模型不会伪造引用？

**答案：**

不能仅靠 Prompt 保证，所以项目采用多层约束：

1. Prompt 要求 Finding 的 URL 只能来自给定来源；
2. 代码建立真实搜索 URL 集合，对模型输出逐条过滤；
3. Finding 必须提供来源原文 quote，程序确认 quote 确实存在并生成内容哈希；
4. Synthesizer 不直接接触任意候选，只接收状态为 `verified + supported` 的 Finding；
5. 引用编号和参考来源列表由代码生成，而不是交给模型自由生成。

仍然存在的边界是：语义支持判定可以约束 quote 与 statement 的关系，但不能证明网页本身正确，也不能替代多来源事实交叉验证。下一步应增加独立评测集、多来源交叉验证和来源可信度评分。

### 11. 如何防御搜索结果中的 Prompt Injection？

**答案：**

项目不再只依赖 Prompt。SourcePolicy 在检索结果进入 LLM 上下文前执行确定性门禁：拒绝危险 scheme、非公网/歧义 IP、内部主机名和嵌入凭据 URL；标题、正文或 URL path/query/fragment 命中“忽略前序指令”“泄露系统提示词”或特权角色标签等规则的来源会被隔离。每次判定以结构化事件记录原因，同时 Researcher 和 Synthesizer 仍把网页明确视为不可信数据。
在论断层面，EvidenceVerifier 的逐字 quote 匹配仍是硬门禁；SemanticEvidenceVerifier 只能在硬门禁通过后补充 `supported/unsupported/uncertain` 状态，不能把未命中的引用提升为可信。ClaimConsistencyVerifier 则在已支持论断之间标记矛盾关系，让系统保留“存在争议”的审计链，而不是让 Synthesizer 随机选择一边。

规则门禁仍不是完整防线：它可能误报讨论 Prompt Injection 的正常文章，也可能漏掉混淆、图片或跨语言攻击；域名当前只做字面检查，没有解析后 IP 与 DNS rebinding 防护。生产强化应加入抓取层网络沙箱、DNS/IP 二次校验、内容分类器、工具最小权限和红队数据集。

### 12. 反思循环会不会只是增加 Token，却不提高质量？

**答案：**

有这种风险，所以项目没有无条件循环：Reflector 必须输出明确缺口和新子问题；每轮最多三个问题；总轮数和 Token 都有限制；补洞是增量研究，不重复首轮问题。

项目还提供工作流对照评估，可以比较启用反思的 deep 与不启用或较少步骤的 quick 在质量和 Token 上的差异。是否保留反思不应靠直觉，而应根据任务类型和评估数据决定。

### 13. 为什么结构化输出不用 provider 的 JSON Mode？

**答案：**

为了兼容不同 OpenAI-compatible provider。项目将 Pydantic JSON Schema 注入 Prompt，稳健抽取代码块或多余文本中的 JSON，再用 Pydantic 校验。解析失败时把具体错误回灌给模型重试；网络或限流异常则指数退避。

缺点是可靠性和 Token 效率通常不如原生 constrained decoding。更成熟的做法是能力探测：支持原生 JSON Schema 的 provider 使用原生模式，不支持时回退当前通用方案。

### 14. 子问题依赖出现环怎么办？为什么不是直接失败？

**答案：**

对于用户正式创建的工作流图，环会被后端拒绝，因为执行语义必须明确。对于 Planner 临时生成的研究子问题依赖，项目选择检测到环后降级为纯并行，以保证研究任务不会死锁或完全失败。

这是不同数据可信级别下的策略差异：持久化工作流属于用户明确配置，应严格校验；一次性模型计划属于软建议，可以容错降级。

### 15. LLM-as-judge 有哪些偏差？如何改进？

**答案：**

常见偏差包括位置偏差、偏爱长文本、自我偏好、评分漂移和对引用真实性检查不足。项目已通过明确四维量表、参考要点和低温度减少随机性，但仍不能把 Judge 分数当绝对真值。

改进方式：

- 使用固定评测集和版本化 rubric；
- 多次评分取均值或多数票；
- 引入不同模型交叉评审；
- 将引用覆盖率、无效 URL 数、响应时延、Token 等确定性指标与主观评分结合；
- 定期用人工标注样本校准 Judge。

### 16. 如何评估并行调度是否真的提升性能？

**答案：**

应记录串行基线与并行版本的端到端耗时、单子任务耗时、最大并发、外部 API 限流率和 Token。理论上无依赖子问题的墙钟时间接近最慢任务，而不是所有任务耗时之和，但实际还受到连接池、Semaphore、Provider QPS 和长尾任务影响。

简历中不要虚构“提升 80%”。可以写“通过 DAG 同层并发降低独立检索任务的串行等待，并建立耗时/Token 观测”，面试时说明需要用固定数据集压测得出具体数字。

---

## 六、围绕亮点三的面试题与详细答案

### 17. 节点失败后，系统如何决定重试、降级还是终止？

**答案：**

决策顺序是：

1. 在 `max_attempts` 范围内执行主 Agent；
2. 失败后按指数退避等待并重试；
3. 重试耗尽且配置 fallback_agent 时执行备用 Agent；
4. 仍失败则将节点标记 failed；
5. failure_policy=continue 时隔离失败，其他分支继续；
6. failure_policy=fail_fast 时将异常传播到根工作流并保存 failed Checkpoint。

这样把“故障处理策略”放在工作流数据中，而不是散落在 Agent 业务代码里。

### 18. 为什么 Checkpoint 要同时保存 Blackboard 和 Workflow Definition？

**答案：**

只保存 Blackboard 不知道它对应哪一版流程；只保存流程不知道已经产生了哪些结果。项目同时保存：

- Blackboard：已经累积的业务状态；
- WorkflowRun/StepRun：执行到了哪里、各节点状态；
- Definition：当时实际执行的步骤、节点和边。

恢复时使用定义快照，避免用户在任务中断后编辑工作流，导致旧状态套到新拓扑上。这相当于保存执行输入的不可变版本。

### 19. 恢复时为什么不能简单从头重跑？

**答案：**

Agent 节点通常包含有副作用或有成本的外部调用，例如 LLM、搜索、数据库写入。从头重跑会重复消费 Token，结果也可能因模型随机性不同而不一致。

项目根据持久化 StepRun 跳过已经 succeeded/failed/skipped 的根流程节点，并使用 Checkpoint 中已有结果继续执行。需要特别区分根流程与嵌套 compose/team 子流程，恢复游标只属于根 Workflow Definition，不能因为共享 Runtime 中存在步骤就误跳过嵌套流程。

### 20. 多实例恢复为什么需要租约，而不是查询 status=running 后直接恢复？

**答案：**

多个实例可能同时查询到同一条 running 任务，如果没有原子竞争机制就会重复恢复。项目使用带条件的原子 UPDATE：只有租约为空、已过期或属于自己时才能写入 owner 和 expires_at，并根据受影响行数判断是否抢占成功。

租约不是永久锁，实例崩溃后会过期，其他实例可以接管；运行期间心跳续约。边界上要考虑数据库时间、租约时长大于心跳间隔、长时间 GC/事件循环阻塞，以及释放租约只能由当前 owner 完成。

### 21. SSE 客户端很慢或断开会发生什么？

**答案：**

EventHub 为每个订阅者建立独立的有界队列，避免多个客户端互相抢事件。队列满时不阻塞主流程，而是丢弃当前事件；Token 增量本身就是瞬态数据，最终报告和详情接口可以恢复完整状态。

在 `run_stream` 快路径中，如果客户端中途断开且任务尚未结束，会取消后台研究，避免无人消费时继续烧 Token。持久化 API 路径则可以让任务独立于单一浏览器连接运行，由 EventHub 支持重连和迟到订阅。

### 22. 为什么 Token 事件不落库？

**答案：**

流式 Token 数量大、粒度细，全部落库会造成写放大和事件表膨胀。项目只实时推送 Token 增量，非 Token 生命周期事件才进入缓冲和数据库；最终报告、总 Token 和关键状态会持久化。

这是事件分级：瞬态 UI 数据不等于审计数据。若有合规要求，可以按固定时间窗口聚合 Token 事件，而不是逐 Token 写入。

### 23. Token 估算与 provider usage 如何同时处理？

**答案：**

流式响应过程中通常暂时拿不到精确 usage，项目按输入和输出字符数增量估算，用于 UI 实时显示。流结束若 provider 返回精确 usage，Tracer 用 `total - estimated + exact` 校准，并减少 estimated_tokens。

TokenBudget 从 Tracer 的累计值同步，避免预算对象和 Tracer 各计一次造成双重计数。边界是兼容端点可能永远不返回 usage，此时系统会明确标记 `tokens_estimated=true`，不把估算值伪装成账单精确值。

### 24. 为什么单 Agent 错误事件不能直接发成 ORCHESTRATOR error？

**答案：**

流式消费逻辑把 ORCHESTRATOR 的 done/error 视为整次运行终态。如果 Researcher 单点失败也使用这个 stage，SSE 会提前结束，而其他分支实际上仍在运行。

因此项目将单步失败归属到对应 Agent stage，只在根工作流失败时发 ORCHESTRATOR error。这体现了事件协议本身也是系统控制契约，不能只把它当日志字符串。

---

## 七、Agent/LLM 岗位常见面试题

### 25. Multi-Agent 一定比 Single-Agent 好吗？

**答案：**

不一定。Multi-Agent 的价值来自职责隔离、并行执行、上下文隔离和独立策略，但会增加 Token、延迟、状态同步和错误传播复杂度。简单问答使用单 Agent 或 quick 流程通常更经济；需要多侧面研究、并发搜索、证据质检时才值得使用 deep 或动态编排。

项目通过多个工作流和自动评估工具比较质量/成本，而不是把 Agent 数量当成效果指标。

### 26. Agent 系统中 Prompt、工具和编排哪个最重要？

**答案：**

三者解决不同层次的问题：Prompt 定义单步行为，工具连接外部世界，编排决定状态、顺序、并发、失败和终止。生产问题通常出现在编排和边界控制，例如重复执行、无限循环、工具超时和不可恢复；仅优化 Prompt 无法解决这些问题。

### 27. Function Calling 与项目中的结构化输出有什么区别？

**答案：**

Function Calling 由 provider 提供工具/参数协议，通常可靠性更好，但兼容性依赖 provider。项目使用 Schema 注入和 Pydantic 校验，适配任意 OpenAI-compatible 端点。二者并不冲突：可以优先使用原生 Function Calling，失败或不支持时回退通用解析。

### 28. 如何控制 Agent 幻觉？

**答案：**

不能完全消除，只能分层降低：

- 输入层：检索真实来源、限制上下文；
- Prompt 层：明确只依据素材；
- 输出层：Schema 校验和字段约束；
- 代码层：URL 白名单、条件解释器、角色白名单；
- 流程层：Reflector、Critic、Fallback；
- 评估层：groundedness、引用覆盖和人工抽检。

项目在引用真实性上采用了 Prompt 与代码校验双保险，但语义真实性仍可继续加强。

### 29. 如何设计 Agent 的记忆？

**答案：**

先区分工作记忆、情节记忆和长期知识：

- Blackboard 是单次运行的工作记忆；
- Checkpoint、历史事件和报告是可回放的情节记忆；
- 外部知识库或搜索系统属于长期知识。

当前项目没有把所有历史自动塞进新 Prompt，因为这会造成上下文膨胀和隐私问题。若扩展长期记忆，应建立检索、权限、过期、摘要和用户隔离策略。

### 30. 如何做 Agent 系统测试？

**答案：**

项目通过依赖注入替换 LLM 和 SearchTool，单测无需网络和密钥。测试层次包括：

- 纯函数：DAG、条件解析、JSON 抽取、Token Budget；
- Agent：给定假模型输出验证 Blackboard 变化；
- 引擎：重试、超时、Fallback、Join、Fail-fast、取消；
- 恢复：Checkpoint 后跳过已完成节点；
- 仓储/API：定义、事件、运行状态的往返；
- 前端：SSE 状态归并和进度计算；
- 评估：固定数据集上的工作流质量/成本回归。

测试不能只断言最终有报告，还应断言执行轨迹、调用次数、状态转换和引用来源。

### 31. 如何降低 Agent 成本和延迟？

**答案：**

- 简单任务路由到 quick 或单 Agent；
- 独立子任务并发，但用 Semaphore 避免限流；
- 反思采用有界轮次，只增量研究缺口；
- 根据角色绑定不同成本模型；
- 结构化摘要中间状态，减少上下文；
- Token Budget 达限后跳过非终端步骤；
- 缓存稳定检索和模型结果；
- 用评估数据决定某一步是否值得保留。

当前项目已实现工作流选择、并发、增量补洞、角色模型档案和 Token Budget，缓存是后续可扩展项。

### 32. 如何处理模型供应商差异？

**答案：**

项目统一使用 OpenAI-compatible 客户端，并将模型、Base URL、API Key、temperature/reasoning_effort 放入模型档案。推理模型模式不发送 temperature，只发送 reasoning_effort；流式端点不支持 stream_options 时对特定错误降级重试。

更完整的 Provider Adapter 还应处理工具调用格式、上下文窗口、错误码、并发限制和 usage 字段差异。

---

## 八、边界问题与高阶追问

### 33. 如果一个并行节点完成后进程崩溃，但 Checkpoint 还没写入，会不会重复执行？

**答案：**

会存在“外部副作用完成、Checkpoint 未提交”的窗口，这是典型的 at-least-once 执行语义。当前项目在每步或每层后保存 Checkpoint，缩小窗口，但无法实现跨 LLM/API 和数据库的原子事务。

解决方向是为工具调用增加幂等键和调用日志：执行前写 operation_id，成功后记录结果；恢复时先查结果再决定是否重放。对于无法幂等的外部操作，需要人工确认或补偿事务。研究型只读调用主要风险是重复成本，而不是破坏性副作用。

### 34. 如果租约执行到一半过期，另一个实例会不会接管并重复执行？

**答案：**

可能，因此心跳周期必须明显小于租约 TTL，并监控续租失败。

当前项目在 owner、过期时间和续租之外，已经实现了基于 owner 校验的写入 fencing：所有受保护的后台写入在同一事务内先以空操作 UPDATE 锁定 workflow_run 行并校验 `lease_owner`，不匹配则抛 `LeaseLostError`；续租失败时执行任务会被直接取消。因此旧实例即使还在运行，也无法把状态提交回数据库。剩余的增强点是把 owner 比较升级为单调递增的 fencing token（可避免同 owner 重启后的 ABA 边界情形），适合在生产规模扩大后引入。

### 35. `dict.update` 合并 scratch 会不会覆盖其他节点结果？

**答案：**

会，这是当前确定性但较粗粒度的边界。按声明顺序合并保证结果稳定，却不能保证业务语义正确。对于并行节点可能写同一 key 的场景，应采用命名空间，例如 `scratch[node_id]`，或让 WorkflowNode 配置 reducer。

面试中应主动说明：确定性不等于无冲突，当前实现优先解决竞态和可复现性，下一步是类型化 reducer。

### 36. 条件路由读取的是层开始前的 Blackboard，会有什么影响？

**答案：**

同一拓扑层的节点不能依赖同层其他节点刚产生的数据，这是 DAG 分层语义的自然结果。需要依赖某节点输出的条件节点必须位于后续层，并通过边建立依赖。

如果业务需要事件级动态触发，应改成就绪队列调度器，而不是固定层调度。

### 37. 某个 Researcher 返回空结果，它算成功还是失败？

**答案：**

搜索成功但没有内容属于业务空结果，不一定是系统失败；网络异常则属于失败。当前 Researcher 对无搜索结果返回空 findings，对检索异常返回 None 并记录 error。调度器只收集有 findings 的结果。

更精细的状态可区分 succeeded_empty、failed_transient、failed_permanent，让 Reflector 和重试策略作不同处理。

### 38. Synthesizer 在 Token Budget 耗尽后仍执行，会不会突破硬预算？

**答案：**

会有可能。当前策略是“软预算 + 保底报告”：预算耗尽后跳过非终端步骤，但允许终端步骤执行，保证用户尽可能拿到结果。因此 max_tokens 不是严格账单上限。

如果业务要求硬预算，应为终端步骤预留额度，或在调用前根据模型上下文和 max_output_tokens 做预算分配，并在客户端支持取消。面试时需要明确软预算和硬预算的区别。

### 39. EventHub 队列满后丢失非 Token 事件，如何保证前端一致性？

**答案：**

当前选择“主任务可用性优先”：不让慢 SSE 消费者阻塞执行。在线视图可能短暂缺事件，但任务结束后可以通过运行详情和数据库事件回放恢复。

更严格方案可以给事件增加 seq，前端发现序号断层后调用 `events?after_seq=` 补拉；也可以只丢 Token，关键状态事件写入单独高优先级队列。

### 40. 当前系统是否已经是完整的分布式任务队列？

**答案：**

不是。它具备后台任务、持久化状态、恢复和租约，但任务仍运行在 FastAPI 进程内，没有独立 Worker、消息确认、调度队列和水平扩缩容控制。

如果任务量增长，应引入 Celery、Dramatiq、Arq、Temporal 或自建队列，将 API 与 Worker 解耦。当前实现适合展示 Agent 运行时核心机制，也为迁移到专业工作流平台提供了状态机和 Checkpoint 基础。

### 41. 如何保证数据库 Checkpoint 和研究结果表的一致性？

**答案：**

当前多个仓储方法各自使用独立事务，因此可能出现 Checkpoint 已保存、业务结果表尚未写入，或反之。恢复主要依赖 Blackboard Checkpoint，最终结束时再将计划、结果和报告落库，所以运行状态具备恢复能力，但数据库读模型可能存在短暂不一致。

增强方案包括：把一次步骤提交涉及的 Checkpoint、StepRun 和新增结果放入同一事务；或采用 Outbox/Event Sourcing，从事件重建读模型。

### 42. 如何处理用户删除或修改正在运行的工作流？

**答案：**

项目在运行时保存 Workflow Definition 快照，恢复继续使用快照，所以修改工作流只影响后续新运行。Catalog 中的工作流定义本身带 version，但当前运行记录只保存 snapshot，不单独保存或回显该 version；面试时不能声称历史运行已经具备版本号追踪。

删除定义时也不应删除历史快照，否则历史运行无法解释。当前快照已经提供了这个基础。

### 43. 如何防止自定义角色的 system_prompt 造成越权？

**答案：**

当前自定义角色只能选择既有 behavior，复用预定义工具和执行路径，因此 Prompt 不能任意获得新工具权限；工作流又通过角色白名单和终端角色校验。

生产环境还应增加角色创建权限、Prompt 审计、敏感信息脱敏、模型与工具 ACL、最大输入输出长度，以及不同租户之间的数据隔离。

### 44. 如果让你继续迭代，优先做什么？

**答案：**

优先级可以是：

1. 为并行 Blackboard 增加字段级 Reducer 和冲突检测；
2. 为 Checkpoint 写入增加 fencing token 和工具调用幂等日志；
3. 将后台运行迁移到独立 Worker/Temporal；
4. 建立固定评测集、人工基准和多来源交叉验证指标；
5. 建立固定评测集、确定性指标和 CI 质量门禁；
6. 增加缓存、模型路由和硬成本预算；
7. 完善多租户鉴权与操作审计。

回答时应根据目标岗位选择重点：Agent 算法岗强调证据和评估，平台岗强调调度恢复，后端岗强调状态机、事务和 SSE。

---

## 九、简历项目描述完整模板

### 项目名称

Deep Research Agent｜可恢复的多 Agent 深度研究与可视化编排平台

### 项目简介

面向复杂研究任务设计的多 Agent 系统，支持问题规划、DAG 并发检索、证据反思补洞、引用报告生成、用户自定义工作流和 LLM 动态编排，并提供节点级可靠性、执行恢复、实时观测及自动化质量评估。

### 三条职责/成果

1. 设计 Workflow-as-Data 编排内核，以 Blackboard 和统一 Agent 协议解耦角色与流程，支持线性/DAG、条件路由、三类 Join、反思循环、动态自组合及多团队 Map-Reduce；采用隔离子黑板与确定性合并解决并发状态竞态。
2. 构建证据约束的深度研究闭环，通过带依赖子问题的拓扑并发检索、真实 URL 白名单、增量反思补洞和系统生成引用降低幻觉；实现 LLM-as-judge 工作流对照评估，量化报告质量与 Token 成本。
3. 完成长任务可靠性与可运维体系，包括节点超时/重试/Fallback/Fail-fast、Token Budget、状态机、Checkpoint、定义快照和数据库恢复租约；基于 Tracer、EventHub 与 SSE 实现实时执行链路、迟到订阅回放及历史审计。

### 可量化指标建议

项目当前代码提供了采集能力，但简历中的具体数字应通过实际压测或评估生成，不要编造。建议补充：

- 固定评测集上 deep/quick/auto 的平均质量分和平均 Token；
- DAG 并发相对串行的 P50/P95 耗时；
- 故障注入下节点重试、Fallback 和恢复成功率；
- 服务重启后避免重复执行的 LLM 调用次数；
- SSE 多订阅者和慢消费者压测下的内存占用；
- 单测数量、核心模块覆盖率和 CI 通过率。

---

## 十、面试表达注意事项

1. 不要只说“使用多个 Agent 提高准确率”，要讲角色边界、状态契约、调度与失败语义。
2. 不要把 LLM 的自我反思描述成绝对正确，应强调有界循环和评估验证。
3. 不要声称完全消除幻觉；准确说法是通过来源白名单、结构化校验和引用生成降低风险。
4. 不要把 TokenBudget 说成绝对硬预算；当前实现允许终端节点保底执行。
5. 不要把当前恢复机制说成 exactly-once；外部调用整体仍是 at-least-once，需要幂等键进一步加强。
6. 不要回避当前边界。主动说明 Reducer、事务一致性、fencing token、独立 Worker 等改进方向，通常比把项目描述得“没有缺点”更有说服力。
